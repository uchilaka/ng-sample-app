{
  "extends": "../tslint.json",
  "rules": {
    "directive-selector": [true, "attribute", "app", "camelCase"],
    "component-selector": [true, "element", "app", "kebab-case"],
    "no-use-before-declare": false,
    // @TODO figure out what is causing the default of double quotes!! Was it from user settings?
    // @TODO figure out which trumps user settings vs. workspace settings
    "quotemark": [true, "single"]
  }
}
